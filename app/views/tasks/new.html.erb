<h1>新規使用アプリページ</h1>

<%= form_with(model: @tasks, local: true) do |f| %>
  <%= f.label :content, '新規アプリ' %>
  <%= f.text_field :content %>

  <%= f.submit '新規' %>
  
   <%= render 'form', task: @tasks %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>